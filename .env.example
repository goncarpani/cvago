# OpenAI (obligatorio para CV Parser y CV Generator)
# Copiá a .env en la raíz del repo o en backend/ y completá los valores.
OPENAI_API_KEY=sk-...

# Modelo para CV Parser y CV Generator (por defecto: gpt-4o)
# OPENAI_MODEL=gpt-4o

# Modelo para resumir la job description (por defecto: gpt-4o-mini, más barato)
# OPENAI_SUMMARY_MODEL=gpt-4o-mini
